# DigiVault — خيارات تخزين الملفات الكبيرة

## الخيار التلقائي داخل الموقع
Firebase Cloud Storage مناسب للرفع من الكمبيوتر والهاتف ويولّد رابط تنزيل تلقائياً. في النسخة الحالية، الملفات الكبيرة لا تُخزن داخل Firestore، والحد الذي يعلنه التطبيق لملف واحد هو 5 TiB. يجب تفعيل Cloud Storage في مشروع Firebase والخطة المطلوبة من Firebase Storage.

## البديل منخفض التكلفة
يمكن استخدام Cloudflare R2 كاستضافة خارجية للملفات، ثم وضع رابط الملف في حقل «رابط التحميل / التسليم الرقمي». وفق التسعير الحالي، R2 يضم 10 GB-month من التخزين القياسي شهرياً، ومليون عملية Class A، و10 ملايين Class B، مع عدم فرض رسوم egress من R2. هذه ليست سعة غير محدودة.

## لماذا لا نضع الملف داخل Firestore؟
Firestore يفرض حداً أقصى قدره 1 MiB للوثيقة، لذلك لا يجوز تخزين الصور أو الملفات الكبيرة كـ Base64 داخل وثيقة المنتج. النسخة الحالية تحفظ في Firestore بيانات المنتج وروابط/بيانات الملف فقط.

## ملاحظة أمنية
رابط التحميل الخارجي هو رابط يحدده البائع. قبل البيع التجاري، يفضّل استخدام استضافة تتيح التحكم في الوصول إذا كانت الملفات المدفوعة يجب أن تكون محمية.
