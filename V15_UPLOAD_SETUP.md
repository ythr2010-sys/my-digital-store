# DigiVault v15 — إعداد الرفع المباشر

## النتيجة

البائع يختار الملف من صفحة إضافة المنتج أو تعديل المنتج، ثم DigiVault يرفع الملف إلى R2 بدون أن يكتب البائع رابطاً يدوياً. الملفات الكبيرة تستخدم multipart upload.

Cloudflare R2 يدعم حالياً كائناً حتى 5 TiB، والـmultipart upload مناسب للملفات الكبيرة ويمكنه إعادة محاولة الأجزاء الفاشلة. إعداد المتصفح في v15 يستخدم أجزاء 50 MiB وبعدد عاملين متوازيين، لذلك الحد العملي للإعداد الحالي يقارب 500 GB قبل تجاوز 10,000 جزء؛ يمكن تعديل حجم الجزء عند الحاجة مع مراعاة ذاكرة الجهاز.

## الخطوات

1. أنشئ R2 bucket خاصاً بالمشروع.
2. أنشئ R2 API token/Access Key مناسباً للاستخدام كـparent credential. لا تضع السر في الموقع.
3. عدّل `upload-worker/wrangler.toml` بالقيم الخاصة بالحساب والـbucket والـAccess Key ID.
4. خزّن السر:

```bash
npx wrangler secret put R2_PARENT_SECRET_ACCESS_KEY
```

5. ضع نطاق الوصول العام للملفات في `R2_PUBLIC_BASE_URL`. الأفضل استخدام custom domain مخصص للتخزين عند تشغيل المتجر فعلياً.
6. اضبط CORS من `r2-cors.json`.
7. انشر Worker:

```bash
cd upload-worker
npm install
npx wrangler deploy
```

8. انسخ رابط Worker الناتج إلى `upload-config.js`.
9. اختبر تسجيل الدخول ثم رفع ملف صغير، ثم ملف أكبر من 100 MB.

## الأمان

- المتصفح لا يحصل على parent secret.
- Worker يتحقق من Firebase ID token.
- Temporary credentials مقيدة بمسار `uploads/<uid>/`.
- العمليات الممنوحة للعميل محدودة بعمليات S3 المطلوبة للرفع.
- Firestore يحتفظ بالـmetadata والرابط/المفتاح فقط، وليس محتوى الملف.

## ملاحظة مهمة حول تنزيل المنتجات المدفوعة

v15 تحل مشكلة **رفع** الملفات الكبيرة. رابط التنزيل العام للمنتج يحتاج إعداد نطاق R2 مناسب، كما أن بوابة تنزيل خاصة مرتبطة بحالة الطلب والدفع هي تحسين أمني مستقل ينبغي إضافته قبل تشغيل متجر مدفوع على نطاق إنتاجي.
