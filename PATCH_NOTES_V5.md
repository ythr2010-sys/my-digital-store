# DigiVault v5

- إصلاح صفحة مشترياتي: قراءة المشتريات بواسطة UID أولاً مع دعم سجلات email القديمة، ومعالجة مستقلة للأخطاء.
- إضافة buyerUid إلى الطلبات والمشتريات المجانية الجديدة، مع إبقاء buyerEmail للتوافق.
- تحديث Firestore Rules لدعم ملكية UID وemail بدون فتح البيانات للآخرين.
- إضافة `download` إلى روابط التنزيل في صفحة المشتريات.
- بدون Firebase Storage: الملفات المحلية الصغيرة تتحول إلى Data URL وتُخزّن في Firestore، والملفات الأكبر تستخدم رابط تحميل خارجي. لا يمكن لمتصفح فقط إنشاء استضافة دائمة لملف كبير بدون خدمة تخزين/خادم.
- صورة المنتج المباشرة محدودة إلى 500KB لتجنب تجاوز حد مستند Firestore؛ ملف المنتج المباشر 700KB.
