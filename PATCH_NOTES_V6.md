# PATCH_NOTES_V6.md

- دعم اختياري لـ Firebase Storage لرفع ملفات كبيرة وإنشاء download URL تلقائياً.
- عند عدم توفر Storage يستمر الوضع الاحتياطي: ملفات صغيرة داخل Firestore أو رابط خارجي.
- دعم عدة صور للمنتج عبر `imageUrls`.
- صفحة `my-products.html` تعرض سبب الرفض وتوفر تعديل المنتج مباشرة.
- `edit-product.html` يسمح للبائع بتعديل المنتج المرفوض وإعادته إلى pending.
- قواعد Firestore تسمح للبائع بتعديل المنتجات pending/rejected مع منع تغيير الملكية.
- لوحة المدير: تعديل المنتج الموجود يغيّر التصنيف فقط، مع فحص/تنزيل الملف قبل الموافقة أو الرفض.
- ملفات PATCH_NOTES السابقة هي ملفات توثيق للتغييرات وليست ملفات تشغيل للموقع.
